import { Button, Dialog, DialogActions, DialogContent } from "ui";

function App() {
  return (
    <main
      style={{
        display: "grid",
        placeItems: "center",
        minHeight: "100vh",
      }}
    >
      Hello world!
    </main>
  );
}
export default App;
